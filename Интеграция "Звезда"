Звезда" — это подход к интеграционному тестированию, где центральный модуль системы тестируется вместе с каждым подключенным компонентом по очереди.

🔹 Суть метода
Центральный модуль (ядро системы) тестируется первым.
К нему последовательно подключаются другие модули:
Проверяется взаимодействие между ядром и каждым компонентом отдельно.
Модули не интегрируются друг с другом напрямую (только через ядро).
После проверки всех пар «ядро-модуль» система собирается полностью.

🔹 Плюсы
✅ Локализация ошибок (проблемы выявляются в конкретной паре «ядро-модуль»).
✅ Уменьшает сложность по сравнению с Big Bang (одновременная интеграция всех модулей).
✅ Подходит для систем с четко выделенным центральным компонентом.
Минусы
❌ Не проверяет взаимодействие между периферийными модулями (например, CRM ↔ Склад).
❌ Требует больше времени, чем подход "Снизу вверх".
