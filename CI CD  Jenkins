CI/CD (Continuous Integration / Continuous Delivery) — это подход к автоматизации процессов разработки, сборки, тестирования и развертывания приложений.
CI (Continuous Integration) — автоматическая сборка и тестирование кода при каждом изменении.
CD (Continuous Delivery/Deployment) — автоматическое развертывание на тестовые/продуктивные среды.
🔹 Аналог: Конвейер на заводе, где каждый этап проверяется перед передачей дальше.

2. Зачем тестировщику CI/CD?
Раннее обнаружение багов (тесты запускаются при каждом коммите).
Скорость — автоматизация рутинных проверок.
Стабильность — меньше «работало вчера, сломалось сегодня».



3. Ключевые этапы CI/CD
Коммит кода → 2. Сборка (Build) → 3. Автотесты → 4. Развертывание → 5. Мониторинг.

Jenkins — это open-source сервер для автоматизации процессов (CI/CD), который управляет задачами (jobs) по расписанию или событию (например, пуша в Git).

Как работает:
Запускается (по событию или таймеру)
Забирает код из репозитория (Git)
Выполняет pipeline (сценарий):
Сборка (mvn install, npm build)
Тестирование (npm test, pytest)
Анализ кода (SonarQube)
Деплой на сервер (kubectl apply, scp)
Отчитывается (лог, уведомление, графики)
